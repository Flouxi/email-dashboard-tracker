// api/index.js
// Vercel serverless entrypoint. Vercel's Node runtime accepts an Express
// app directly as the exported handler (it's callable as (req, res)).
module.exports = require('../server');
